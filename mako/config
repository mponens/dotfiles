border-radius=0
border-size=1
border-color=#6f9fa0
default-timeout=5000
background-color=#2f3030
text-color=#d8d8d8
padding=6
margin=2
text-alignment=center
group-by=
[app-name="audioNotifications"]
#ensure that we can see audio changes even over things like fullscreen
layer=overlay
background-color=#2f3030
#this is the default configuration minus the (%g) so that the number of notifications don't show
format=<b>%s</b>\n%b
[app-name="brightnessNotifications"]
layer=overlay
format=<b>%s</b>\n%b
